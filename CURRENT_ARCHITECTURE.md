# TimeTrace 当前架构

本文按当前源码说明实现；验证结果见 [实施与验证报告](IMPLEMENTATION_REPORT.md)，领域词汇见 [CONTEXT.md](CONTEXT.md)。

## 产品与模块边界

时光落点是以“工作”为默认活动的地点时间记录应用，保留今天、统计、地点、历史、设置五个页面。

```text
TimeTraceApp → AppContainer → RootStore / 各页面 FeatureStore
                                      ↓
                                   AppModel
                        ┌─────────────┼─────────────┐
                 EventPipeline   Repository    平台服务
                        ↓             ↓       ├─ CoreLocationGeofenceService
              ActivitySessionEngine SwiftData └─ LocalNotificationService
                                      ↓
                                可选私有 CloudKit

ActivitySession → AnalyticsService → 今天 / 历史 / 统计
```

- `AppContainer` 为同一个 `AppModel` 组装各页面 Store，由 `TimeTraceApp` 注入页面；仓库和平台服务的创建、注入入口目前位于 `AppModel` 初始化器。
- `FeatureStore` 目前转发 `AppModel.objectWillChange`；页面仍通过 `store.application` 调用应用用例、读取 SwiftData 模型。它尚不是完整的独立页面模块。
- `AppModel` 编排引导、地点、手动记录、提醒、同步刷新和发布状态。页面不直接操作 `ModelContext`。
- `EventPipeline` 追加事件、调用会话引擎、清理重复投影并保存结果。
- `ActivitySessionEngine` 负责事件配对、过期规则、人工修正和删除；`AnalyticsService` 只计算统计结果。
- `TimelineValueTypes` 提供不可变值与映射器，目前尚未替代页面中的 SwiftData 对象。
- `ScreenTimeService` 只有协议与测试用 Stub；没有接入实际屏幕使用时间限制。

工程为一个 App Target 和一个 Test Target，无第三方运行时依赖，不拆分独立模块。

## 模块调用约定

| 操作 | 入口和调用链 | 结果如何回到页面 |
| --- | --- | --- |
| 权限指引 | 今天 / 地点 / 设置 → `TTLocationPermissionNotice` → 系统设置 | Core Location 授权回调更新 AppModel，FeatureStore 转发变更；回前台由 RootStore 刷新数据与设备注册 |
| 地点编辑 | `WorkplaceEditorView` → `AppModel.updateWorkplace` → `updatePlace` → 活动仓库 → 围栏服务 | 名称、坐标、类型、启停和工作排班由同一用例保存；仅排班变更时选择“仅今后 / 全部历史”，成功才关闭编辑页 |
| 提醒管理 | ReminderManagementView / ReminderEditorView → AppModel 创建、修改或删除定义 → 提醒仓库 / 通知服务 | 发布定义列表；保存成功关闭编辑页；权限不足或排程失败显示设置指引 |
| 自动记录 | 围栏服务回调 → AppModel 校验有效且非演示地点 → EventPipeline → 会话引擎 / 仓库 | 重新读取并发布会话；今天、历史、统计使用同一数据源 |
| 备份导出 | DataBackupView → `AppModel.backupSnapshot` → DataBackupService | 返回值快照，由 BackupArchive 在后台任务加密，再交给系统文件导出器 |
| 备份导入 | DataBackupView 解密并确认 → `AppModel.importBackup` → DataBackupService 合并 | 应用用例重放事件、恢复围栏、发布数据并异步核对通知；已保存但刷新失败时返回独立提示 |
| 云端 / 前台刷新 | RootStore → AppModel → 仓库读取 / 重放 / 围栏恢复 / 通知核对 | FeatureStore 转发 AppModel 的变更；没有逐次云合并监听 |

页面不直接调用持久化服务。文件选择、密码输入、加解密任务及分享弹窗属于备份页面的文件交互；数据库合并及业务刷新由 AppModel 统一负责。FeatureStore 仍是共享模型的观察转发层，不是各自拥有数据库的独立模块。

## 默认活动与引导

默认工作活动取按创建时间排序的第一个 `.work` 活动，用于新增地点及手动记录。地点列表、历史、异常记录和统计读取所有 `.work` 活动的数据；不同数据库导入或同步后仍保留原始活动 UUID 及关联，已有记录不会因默认活动改变而隐藏。是否进入主界面以该活动是否存在判断，不以地点数量判断。删除最后一个地点后，仍能访问原有历史和设置；再次配置地点会复用已有工作活动。

首次设置和地点编辑页共用地点名称与地址搜索的焦点状态；点击表单空白、轻点地图、滚动表单、提交搜索或选择结果时收起键盘，地点名称输入支持“完成”收起。空白点击使用不取消控件触摸、允许同时识别的手势，并排除输入框及其他 UIKit 控件，避免拦截类型菜单或打断输入。

地址搜索使用 `MKLocalSearch` 同时检索具体地点（含出入口）和地址，以所选城市及周边区域作为搜索参考；关键词已包含所选城市时不重复添加城市名。手动城市定位仍使用 `MKGeocodingRequest`，不用于具体地点搜索。

地点编辑页支持启用或停用自动记录；停用保留历史，不自动补造进行中会话的结束时间。编辑停用地点或演示地点不会注册真实围栏。

地点可以标记为工作、学习、运动、居住、餐饮、购物、医疗、休闲或其他。当前地点页面创建的地点仍属于默认工作活动；`addPlace(activityId:...)` 用例支持其他活动，但没有完整的活动切换界面。工作类型地点可编辑工作日和上下班时间；新增工作地点优先沿用已有工作地点排班，无参考时默认周一至周五 09:00–18:00。排班仅用于历史正常工时和加班统计，不限制围栏记录，也不自动结束会话。

## 事件与会话

- 业务输入追加 `ActivityEvent`。仓库按事件 UUID 防止重复插入；处理状态 `disposition` 会在回放中更新，因此整个模型对象并非完全不可变。
- 同一活动的地理围栏事件按 `placeTriggerId` 分别配对；无地点的手动和提醒事件使用独立的无地点状态。
- 自动生成的新会话使用开始事件 UUID 作为会话 UUID。旧会话的 UUID 保留；同一开始事件产生的多个投影会先合并，避免重复统计和字典重复键崩溃。
- 合并优先保留已删除或人工修正的投影，再按创建时间和 UUID 稳定选择；`EventPipeline` 删除多余会话投影，不删除原始业务事件。提醒关联可以通过开始事件中的提醒实例标识回查会话。
- 人工修改和删除写入带 `sessionId`、`startEventId` 的事件。最新修改决定人工边界；用户删除保留软删除标记。
- 人工补齐结束时间后，在该边界释放运行状态，不阻塞下一次到访。保留空结束时间的旧修改不会覆盖修改之后发生的真实停止事件。
- 缺少到达时间的异常修复由 `AppModel.repairOrphanedExit` 追加带 `repairsEventId` 的手动开始事件，并直接保存与指定离开事件绑定的人工会话。这条路径并非完全由普通回放重建。
- 所有围栏会话从首次进入持续到最终离开，超过 24 小时或跨零点也不重置；无地点的手工及提醒会话仍在 24 小时后标为不完整，不伪造结束时间。
- 工作围栏进入事件携带当时的 `WorkScheduleSnapshot`、地点类型和时区。排班历史修正复用 `sessionAdjusted` 事件且标记 `adjustmentKind=workSchedule`；引擎不把它视为时间边界修正，历史页也不因此标记“后补/修正”。
- 跨午夜不拆分，统计按开始时间归属日期。用户删除记录不会删除其原始事件；Debug 清除演示数据会实际删除带演示标识的事件与会话。

普通事件追加与投影保存是分步持久化。地点排班变更例外：触发器更新与该地点的排班修正事件由同一 `ModelContext.save()` 批量保存，失败时回滚，随后再重放会话投影。重复投影回归测试覆盖人工模拟的分批到达；真实 CloudKit 到达顺序仍需真机验收。

## 本地存储与 iCloud

`PersistenceController` 打开应用支持目录下的 `TimeTrace.store`，使用同一 Schema 保存活动、触发器、事件、会话、证据、提醒定义和提醒实例。

初始化时存在 iCloud 身份且不是内存测试库，才配置私有数据库 `iCloud.com.chronora.time.trace`；否则使用本机配置。登录状态改变后，存储配置不会在进程内自动切换，需要重新启动应用。设置页显示的是容器配置和账户可用状态，不表示所有记录已经上传完成。数据备份页的 iCloud 同步范围仅列出活动与地点、时间记录和已有提醒，不展示不参与同步的项目。

存储初始化失败时，应用使用 `UnavailableRepositories` 提示不可用，不创建新的空库覆盖原数据。

刷新入口包括启动、回到前台、账户状态检查成功以及启动后的延迟刷新。目前没有订阅每一次 CloudKit 合并通知；不能承诺远端更改一到达就立即更新页面。

每次数据刷新会重读活动和地点，对所有已读取的活动重放会话；恢复启用且非演示的围栏：配置未变时复用系统注册并查询状态，仅坐标、有效半径或进出通知配置变化时重新注册，移除仍存在于本地配置中的停用或无效围栏。若系统状态查询返回围栏外而会话尚未结束，当前运行期间提示“可能漏记离开”，保留原会话和真实时间边界；用户补齐或真实退出结束该会话后消除提示。状态查询不伪造退出事件。`Library/Caches/GeofenceDiagnostics.json` 仅保留最近 200 条回调、注册、处理结果与数字错误码，不记录坐标、地点名称、用户或地点标识。缓存可能被系统清理，不等同于永久审计日志。远端已删除地点的旧系统注册尚无完整清理扫描，但回调会因找不到有效地点而被忽略。

空库且 iCloud 可用时先显示恢复选择。用户选择恢复后按 1、3、6 秒间隔再次读取；仍无默认活动则回到选择页。选择开启新记录不会删除云数据。常规启动也会按 2、6 秒间隔刷新；这些等待不是“同步已完成”的保证。

## 文件备份

设置 → 数据备份通过 `DataBackupService` 导出七类持久化模型，包括软删除记录、事件和辅助证据。`BackupArchive` 使用 PBKDF2-HMAC-SHA256（600,000 轮、随机盐）和 AES-256-GCM 加密，界面要求导出密码至少 8 个字符；文件由系统文件导出器保存。

导入先解密并确认，再按各模型 UUID 新增缺失记录，相同 UUID 保留本机版本；保存后由 `AppModel.importBackup` 刷新数据、重放事件、恢复围栏并异步核对通知。合并已保存但后续刷新失败时，返回“数据已保存、刷新未完成”的提示，不承诺设备服务已全部恢复。因此新增事件可能改变会话投影。昵称、系统权限与系统围栏注册不在备份中。文件导入限制为 100 MiB。文件选择与真机恢复流程仍需设备验收。

## 提醒与通知

设置 → 活动提醒 → 管理提醒提供添加、编辑、启停和删除入口。新增时选择活动类型；编辑保留原活动关联，只修改提醒名称、时间、重复日期和启停状态。名称不能为空，重复日期至少选择一天。保存或删除失败时不关闭编辑页。

`ReminderDefinition` 是通知计划的唯一依据。新增不再生成重复的 schedule 触发器；旧数据中的 schedule 触发器不参与通知排程，删除某活动最后一个提醒时清理这类旧触发器。编辑先保存定义、取消本机旧请求（含延后及已投递通知），再根据当前定义重新安排。删除或停用保留活动、提醒实例与会话，进行中的活动仍可在今天页完成。

通知权限未授权时，提醒保存成功但不会保证发送，管理页提供系统设置入口；返回前台重新检查权限，应用刷新时重新核对通知。数据备份页的同步范围列出提醒设置和执行记录。

`LocalNotificationService` 使用按星期重复的本地通知，支持开始、延后 10 分钟、今天跳过和明确关闭通知。普通点击按开始处理。

- 每次可观测投递使用“系统请求标识 + 实际投递时间”作为实例键；不同周不会复用旧实例，同一次投递的重复开始操作不会重复创建会话。
- 前台投递或通知操作回调才会产生应用可观测事件；后台静默送达且无人操作时，没有保证生成实例。没有回调也不会被自动判为已忽略。
- 通知定义变化后进行完整请求差异同步：移除已删除或停用定义的待投递及已投递通知，包括延后提醒；保留其他定义的通知。
- 本机删除提醒也会取消它的延后通知。排程与取消串行执行，避免彼此穿插。
- 回到前台及同步刷新后重新读取提醒定义并核对请求，即使提醒列表为空也会清理残留请求。
- 通知开始后创建会话，今天页提供完成入口；应用用例支持放弃，但当前页面未提供单独的放弃按钮。

## 页面数据口径

今天与设置共用自动记录状态说明，根据启用地点和定位能力提示配置要求；不把权限或配置状态当作系统必定送达事件的保证。休息日仍记录地点进出。今天、地点和设置页在未获“始终”定位权限时共用可点击提示，整条提示可打开应用的系统设置；权限恢复后提示随授权状态更新而隐藏。

- 今天页主卡通过 `TodayHeroSummary` 随当前进行中地点的类型切换时长；进行中的工作或居住会话显示“本次工作时长 / 本次在家时长”，从原始进入时间连续计时并显示完整到达日期；结束后恢复今天累计口径。居住等非工作地点不混入工作时长。
- 同一地点连续进入保留第一次进入时间，连续离开用最后一次离开更新同一会话，没有去重时间窗口；再次进入开始新会话。跨日未结束的围栏记录保留原进入，后续离开仍可配对。手动修正和删除优先；无任何可配对进入的离开仍为待补齐。加载与同步刷新时从事件重建会话与处理标记，使旧记录和云端合并遵循同一规则。
- 历史展示最近六个月的已结束日汇总，并单独列出进行中、待补齐及孤立离开事件。会话仍归入进入日且不拆分；跨越的自然日边界按会话时区显示“跨 N 天”。工作卡片显示加班合计，详情显示总时长、正常工时、加班总计及非零的早到、晚走、休息日加班；手工记录、非工作地点、排班关闭或无法恢复排班时仅显示停留时长。修改边界后即时重新计算。
- 学习、运动等其他地点类型进行中仍展示当天累计时长。所有原始会话都不按零点拆分。
- 统计卡片以明确的地点类型生成摘要：已工作多久、平均每天工作多久、最晚下班、平均下班时间，以及在家时长、最晚到家、最早离家，其他类型展示各自时长。卡片和分享图共用分组布局：总时长使用大字号数字与小字号单位，日均次一级，钟点和日期分层显示；工作与居家各自成组，智能文案置于数据下方。当前范围包含三种及以上类型时，切换为每类一格的总览（屏幕两列，大字体一列，分享三列），每格突出时长，并显示工作日均、最晚到家或其他类型的单次最长；点选格子应用类型筛选，再展示该类完整总结，分享同步当前筛选。总览小时数向下保留一位小数，证据与详情保留分钟精度。屏幕支持深色与辅助功能大字体，分享采用同主题的固定排版，采用总结与趋势长图输出及可识别二维码；工作日均按记录完整的工作日期计算，同日多段先合并，未记录日期不当作零工时；最晚与平均下班取这些日期的末次离开，跨午夜保留次日偏移，按本地钟点而非 DST 当天的绝对秒数计算。时长仅计已结束记录，进行中记录单独提示；时间极值附实际日期，工作跨午夜离开按该次开始日排序，最晚到家以本地 06:00 为跨夜分界，00:00–05:59 排在前一晚之后；最早离家仍按实际本地钟点比较。只调整比较顺序，实际日期展示、原始记录及统计日期归属保持不变。不把未标记或已删除地点推测为工作或家。筛选和分享使用同一不可变摘要快照。
- 统计支持今天、近三天、本周、上一周、最近 30 天和自定义日期范围，按地点类型筛选。`TimeJournalService` 生成本地手记与统计证据；`InsightsFeatureStore` 组装同源汇总，交给 `DailyInsightSummary` 管理智能文案。
- `InsightAPIClient` 使用 CloudKit `userRecordID` 换取访问令牌，统计请求使用 `/v1/llm` 的 `scene=insights_summary` 专用模式，不传 `model` 或 `messages`；响应直接解析结构化手记。令牌保存在本机 Keychain，最长缓存 24 小时，提前 60 秒视作需刷新。LLM 请求遇到 401 只重新鉴权一次；409 按 `Retry-After` 最多取回两次结果（单次等待不超过 30 秒），其他失败不自动重发；界面区分网络、鉴权、服务限制和响应格式错误，日志只记录失败阶段、HTTP 状态和白名单中的服务端错误码，不记录响应原文或用户标识。
- 当天、本周、近三天及最近 30 天在一次请求中生成，只提交各范围内有记录的类型。范围 ID 使用小写下划线。线上接口当前拒绝上一周等纯历史范围及超过 366 天的范围，这些范围保留本地文案并从请求中排除。周期手记结果、请求和失败状态不持久化，每次 App 重启后首次进入统计页重新请求。每日关照单独按已验证账户、时区和日期持久化，重启可复用；新增记录不轮换当天文案，来源删除或修改后失效。单次运行内按日期和时区去重，切换筛选不重复请求，并合并并发点击。失败时提供“重试智能手记”，提交最新汇总和当天幂等键（V2 联调阶段服务端关闭每日配额和缓存，每次 HTTP 请求都会重新调用模型）。服务端返回 `502 GENERATION_FAILED` 时回退本地文案并允许手动重试。包含当前日期的自定义范围只在当天首次请求选中时附带，未命中精确范围的筛选使用本地文案。界面显示首次数据截止时间，统计数字仍随本地记录更新。账户变更会取消旧请求并清除内存文案、当前账户关照缓存和令牌。
- 时长按后端 `int64` 协议在编码时向下取整为整数秒，分类与各范围汇总统一处理；读取旧缓存时兼容小数秒，本机原始记录精度不变。汇总只包含日期范围、时区、类别、完成时长、数量等统计值，不上传地点名称、坐标或原始事件。后端仅生成文案，不修改本机业务数据。真实 CloudKit 鉴权和服务端生成联调与模拟测试属于不同验证边界。
- 闭合且未删除的会话计入总时长；不完整日的已知时长计入总量，但不计入平均每日时长。跨日会话整段归于开始日，重叠地点的时长没有做时间区间并集去重。

## 当前边界

没有版本化 Schema/显式迁移计划，没有通用活动管理页面、滚动通知预排程、连续轨迹或真实 Screen Time 功能。`ChinaWorkCalendar` 仅内置 2026 年节假日与调休表，其他年份回退为普通周末判断。

模拟器测试不证明实际围栏后台唤醒、通知系统送达、卸载重装恢复或跨设备同步。这些结论必须以真机测试另行记录。

- 统计首屏由每日关照、周期/类型筛选和洞见主卡组成。主洞见按同期变化、重复到访、最长已完成片段排序；没有候选事实时展示本地记录回顾。`InsightSummaryRequest` 新增最近 7 个已结束自然日汇总及匿名候选事实；`dailyTip` 和 `insight` 独立容错，事实指纹不匹配时不用旧洞见。详情和图表保持本地实时数据。新增字段和联调边界见 [每日关照与洞见接口约定](INSIGHT_CARE_API.md)。
- 统计筛选采用单行时间范围/地点类型菜单，自定义日期使用弹层。分享入口以 `InsightShareSnapshot` 捕获当前 `TimeJournal`、`PeriodInsightCopy` 及趋势的统计数据、指标、类型、日历和参考时间，`JournalPoster` 复用页面的 `PeriodInsightCard`，统一标题、正文、指标和全局主题配色；导出时隐藏证据跳转入口和具体地点名称，分享页不提供地点名称开关。统计分享在总结卡片下复用页面的 `PlaceTrendChart`，保留当前指标、均值线和不完整记录提示；日期槽使用快照参考时间，避免跨日后图表变化。海报使用 360pt 固定宽度、内容自适应高度、标准字号和浅色环境，以 3 倍比例导出宽 1080px 的图片。标题、总结、趋势与品牌之间采用固定间距，不用弹性留白撑高。分享页加载时仅生成图片预览，预览直接展示待分享 PNG；只有点击“分享图片”才打开系统分享面板。海报上下各保留 28pt 留白，顶部为“我的时间总结”，中部采用分享专用间距和字号，底部展示品牌图标与产品用途；右下角提供 App Store 下载二维码（App ID `6808740130`），保持黑白对比和四模块静区，仅编码产品下载地址，不携带用户或记录信息。旧版 `InsightCopy` 封面保留为独立组件，不再用于统计分享。

- 全局外观由 `TimeTraceApp` 读取本机 `appTheme` / `appAppearance` 偏好，通过 `timeTraceDesign` 环境值与 `preferredColorScheme` 下发；设置页负责选择，界面保留视图身份并即时更新。五套主题各有浅色/深色语义颜色，分享渲染显式注入所选主题与浅色环境。
- 桌面图标通过 `AppTheme.alternateIconName` 映射到四套备用 App Icon，暖砂传 `nil` 恢复主图标；Debug/Release 均由 `ASSETCATALOG_COMPILER_ALTERNATE_APPICON_NAMES` 声明资源。设置页通过本机 `desktopIconFollowsTheme` 偏好控制“桌面图标跟随主题”，默认关闭；开启时立即同步当前主题，开启期间点选主题由 `ThemeIconController` 调用系统 API。控制器先检查开关，再检查支持能力与当前图标，串行提交，失败保留界面主题并提供重试。关闭时清除错误提示并保留当前图标；切换进行中禁用开关以避免状态竞争。启动 App、恢复偏好及切换浅色/深色模式不主动触发系统图标提示。
- iOS 26 导航保留系统 Liquid Glass，底部标签栏不覆盖不透明背景；主要操作使用原生 `glass` / `glassProminent` 按钮样式。`TTCard` 与统计指标卡通过 `timeTraceCardSurface` 使用轻磨砂材质、主题色覆层和细高光边缘，页面背景为静态主题光晕。内容卡片在“降低透明度”或“增强对比度”开启时回退到实色表面，避免影响文字阅读。
