# TimeTrace 修复与验证报告

核对日期：2026 年 9 月 8 日。本文记录本次代码审查后的修复和验证，不代表 App Store 已发布这些改动。

## 提醒管理恢复

验证：**82 项 XCTest 通过，0 失败，0 跳过**；Release 通用 iOS 模拟器构建、文档链接和 `git diff --check` 通过。结果包 `/tmp/TimeTraceReminderCRUD-20260908.xcresult`，构建日志 `/tmp/TimeTraceReminderCRUD-release.log`。界面完整点击及系统通知实际送达尚未真机验收。

设置页恢复活动提醒管理入口。可新增（名称、活动类型、时间、重复日期），编辑名称 / 时间 / 重复日期，启停和确认删除；已有提醒保留原活动关联。未授权时仍保存定义，管理页提供系统设置入口。修改先取消本机旧普通、延后和已投递通知再排程；删除保留已有活动记录，进行中的实例仍可完成。

新增用例验证完整增改停删链路、未授权保存、输入校验，以及实际通知适配器替代旧时间、星期和延后请求的行为。新增提醒不再生成重复的 schedule 触发器；旧触发器保留兼容，但不用于排程。

## 本轮一致性检查（2026 年 9 月 8 日）

本轮以工作区现有代码为准，保留检查前已有的加密备份改动。核对五个页面、引导、应用用例、围栏、通知、持久化、备份与项目文档。

| 原有不一致 | 修正与证据 |
| --- | --- |
| 休息日提示不自动记录，但围栏仍记录 | 今天页改为休息日仍记录；今天与设置共用地点及权限状态说明 |
| 无地点或权限不足仍提示开启，设置有恒为开启的无效开关 | 去掉固定开关，根据配置与权限显示可执行指引 |
| 提示启用地点但无可见入口；编辑停用地点重新注册围栏 | 编辑页增加启停开关；统一注册入口跳过停用及演示地点；应用用例回归覆盖 |
| 所有地点卡片固定显示启用中 | 根据实际数据区分已启用、已停用、演示地点 |
| 非工作地点在历史、地点、全部统计中被称为工作 | 使用记录、停留时长等中性名称；今天仍明确只显示累计工作时长 |
| 常规安排只有保存功能，但未说明用途边界 | 引导明确不参与工作日判断、自动结束或工时扣除 |
| 提醒管理与数据说明不一致 | 已恢复添加、编辑、启停和删除入口；实例仍按可观测投递创建，文档同步更新 |
| iCloud 文案暗示登录恢复后必然自动接入或创建新库 | 统一为恢复权限并重启后检查现有数据库配置 |
| 文件备份实现未进入架构和隐私说明 | 补齐加密、UUID 合并、原始事件、重放、密码与真机边界 |
| 地址搜索提示结果必在指定城市 | 改为选择结果并在地图确认位置，与区域偏好搜索实现一致 |

上一轮 XCTest：**77 项通过，0 失败，0 跳过**（iPhone 17 Pro / iOS 26.4.1 模拟器）。包括已有备份加密与合并测试，以及新增的地点配置、权限变化、停用后编辑不重新注册围栏回归。该轮结果包为 `/tmp/TimeTraceConsistency-final-20260908.xcresult`，DerivedData 为 `/tmp/TimeTraceConsistency`，其余参数与下方历史命令相同。

本轮最终 Release 通用 iOS 模拟器构建通过（日志 `/tmp/TimeTraceConsistency-release-final.log`）；Markdown 本地链接检查及 `git diff --check` 通过。尚未进行真机后台定位、通知送达、跨设备恢复和文件选择器完整点击验收。下面“此前修复”保留历史依据。

## 模块关系复核

最终验证：**79 项 XCTest 通过，0 失败，0 跳过**；Release 通用 iOS 模拟器构建、Markdown 本地链接检查及 `git diff --check` 通过。结果包：`/tmp/TimeTraceModuleConsistency-final-20260908.xcresult`；构建日志：`/tmp/TimeTraceModuleConsistency-release.log`。这些结果不代表完成真机权限跳转、后台围栏、通知送达或跨设备同步验收。

- 备份持久化服务改为 AppModel 私有依赖；界面通过 `backupSnapshot` / `importBackup` 调用，导入用例负责重放、发布和设备服务恢复。合并成功与刷新失败分别报告。
- 地点编辑将启停状态和地点属性交给同一保存用例，新增 / 修改 / 删除均返回结果；保存失败保留编辑页面。演示地点不接受真实围栏回调。
- 今天、地点和设置共用可点击的权限提示；授权回调经共享模型更新页面，前台刷新恢复设备注册。
- 架构文档增加入口、服务、数据和刷新关系表；更正 AppContainer 与 AppModel 的依赖组装职责，以及底层周期比较并未展示于页面的边界。
- 新增备份跨模块导入测试和地点编辑状态测试，覆盖事件重放、FeatureStore 读取、围栏恢复、提醒核对及重复导入。

## 此前修复

| 问题 | 当前处理 | 验证方式 |
| --- | --- | --- |
| 相同开始事件的重复会话导致崩溃 | 按开始事件合并投影，新会话使用稳定标识，清理多余投影 | 引擎重复输入测试、SwiftData 分批加入重复投影测试 |
| 删除最后一个地点后卡在引导 | 是否已配置由默认工作活动判断，重新配置复用旧活动 | 删除最后地点并再次添加的应用用例测试 |
| 手动补齐结束后吞掉后续到访 | 按修正结束时间释放运行状态，并消费原停止事件 | 后续进出与从空投影重建测试 |
| 修改进行中的开始时间后无法结束 | 旧空结束值不覆盖修改之后的实际停止事件 | 连续多次回放测试 |
| 云端删除提醒后本机仍弹旧通知 | 数据刷新后核对全部通知请求，空定义列表也清理 | 真实通知适配器配合替代系统请求存储测试 |
| 不同周复用旧提醒实例 | 实例键加入实际投递时间，同次开始操作去重 | 跨周投递和重复开始测试 |
| 删除提醒未取消延后通知 | 普通、延后及已投递通知按定义一起清理 | 排程、延后、删除完整路径测试 |
| 历史详情编辑后数据过期 | 修改或删除成功后关闭详情，返回重算列表 | 调用链检查及编译验证，未做自动化点击验收 |
| 定位未完成时搜索按钮卡住 | 所有入口先验证城市状态，再设置搜索中，并用 defer 复位 | 键盘与按钮入口检查及编译验证 |
| 首页把非工作时长显示成工作 | 工作时长仅包含默认工作活动下的工作地点及无地点手动会话 | 数据口径回归测试与页面源码核对 |
| 设置页数量显示代码原文 | 修正字符串插值 | 源码检查及编译验证 |
| 设置页将提醒触发器算作地点 | 地点数量仅统计地理围栏触发器 | 源码检查及 Release 编译验证 |
| 本机存储提示会自动恢复云同步 | 单独提示检查 iCloud 登录并重新启动应用 | 对照存储初始化与状态检查调用链 |

保留现有五个页面、导航与视觉结构。没有进行全局架构重写，也没有向 App Store 发布这些改动。设备备份保留在本机，并通过 `.gitignore` 排除。

## 回归验证

环境：Xcode 工具链、iPhone 17 Pro、iOS 26.4.1 模拟器。

首次加入的三个回归用例在修改前执行失败，共 12 处断言失败，覆盖人工结束、修改进行中记录和删除最后地点。

此前完整 XCTest：**74 项通过，0 失败，0 跳过**。包括既有手动两次事件入库、通知冷启动、人工修改与软删除、地点配对、统计，以及新增的重复投影、通知请求清理和今天页工作时长测试。随后修正的设置页计数与提示通过 Release 编译验证。

测试命令（设备标识使用本机实际可用模拟器）：

```bash
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer \
xcodebuild -project TimeTrace.xcodeproj -scheme TimeTrace \
  -destination 'platform=iOS Simulator,id=DAE0D2F9-C22B-4335-8C19-5F784B56B8BF' \
  -derivedDataPath /tmp/TimeTracePushReview \
  -resultBundlePath /tmp/TimeTracePushReview-20260908.xcresult \
  -parallel-testing-enabled NO test
```

结果包路径是本机验证产物，不随仓库分发。重复运行时应选择未使用的结果包路径。首次环境准备与构建耗时不计入用例执行耗时。

Release 模拟器构建：**通过**（通用 iOS 模拟器目标）。`git diff --check` 通过。构建仍有原有 `CLGeocoder` 弃用警告；本次未迁移地址反查 API。

## 文档同步

README 说明可见功能和运行方式；CURRENT_ARCHITECTURE 说明真实模块依赖、同步触发点、事件与投影边界；MVP_ARCHITECTURE 保留实现规则摘要；App Store 资料和隐私政策与当前功能范围对齐。文档采用简体中文，代码标识保留原名。

## 尚未由本次验证证明的行为

- 真实 CloudKit 的多设备导入顺序、卸载重装与账户切换恢复；测试只模拟重复投影输入，不代表完成双设备验证。
- 真机后台围栏唤醒、系统通知送达、强制结束应用后的行为。
- 历史详情关闭、键盘搜索和文案的完整真机点击验收。
- App Store 当前构建关联、审核或线上发布状态。本次未修改远端资料。

已知实现边界见 [当前架构](CURRENT_ARCHITECTURE.md)：包括有限次数的云刷新、尚未完全收口的页面 Store、没有滚动通知排程、原始业务事件保留，以及屏幕使用时间仅有 Stub。
