# 每日关照与周期洞见接口约定

本次实现 iOS 客户端与兼容协议，不包含服务端实现或部署。现有 `POST /v1/llm`、鉴权流程、`scene: insights_summary`、`schemaVersion: 2` 和旧 `copy` 保留；服务端需允许下面的可选新增字段。上线联调前，新能力使用本地事实与文案，不能把客户端测试视为线上生成成功。

## 页面与时间口径

- 今日小 tip 位于筛选上方，依据当前时区最近 7 个已结束自然日的全部类型记录。今天的数据不参与，切换周期与类型不改变 tip。
- 主洞见依据所选日期与类型，优先展示有意义的同期变化，其次重复到访，最后最长已完成片段。无候选事实时由客户端展示简单回顾，不要求模型编造发现。
- 同期变化只比较双方相同已结束自然日进度；双方至少各有 3 个记录日、差值至少 1 小时且达到前期总时长的 15%，相关范围没有未结束记录时才形成候选事实。
- 重复到访要求同一有效地点至少出现于 3 个不同自然日，不表示连续打卡，也不能称作某一类型的全部活动天数。最长片段只从已完成记录选出。
- 跨日记录按开始日归属，完成时长整段计入，不拆分午夜或去除重叠时间。未结束、结束时间无效或晚于当前时间的记录不贡献完成时长。
- 历史及接口不支持的自定义范围保持本地洞见。当前请求一次携带今天、本周、近三天和最近 30 天；当日首次请求选中的受支持自定义范围可一并携带，不因切换筛选再次生成。

## 请求新增字段

以下是增量示例，合并到原有完整请求中。示例指纹是占位值；实际为客户端生成的 64 位 SHA-256 十六进制字符串，服务端只需原样回传，无须重算。

```json
{
  "recentSourceID": "<recent-source-sha256>",
  "recent": {
    "id": "recent_completed_days",
    "kind": "range",
    "startDate": "2026-09-04",
    "endDateExclusive": "2026-09-11",
    "completedDurationSeconds": 10800,
    "recordCount": 3,
    "unfinishedCount": 0,
    "recordedDays": 3,
    "types": [
      {
        "type": "exercise",
        "completedDurationSeconds": 10800,
        "recordCount": 3,
        "unfinishedCount": 0,
        "recordedDays": 3,
        "facts": []
      }
    ],
    "facts": []
  }
}
```

`recent` 是单独的 tip 上下文，允许完全位于今天之前；不能套用现有 `periods` 必须包含今天的校验。范围包含开始日、不包含结束日，按请求的 `timezone` 解释。`recentSourceID` 绑定这份摘要的本地来源，防止服务端幂等返回旧结果后被客户端保存。

`day`、`week`、每个 `periods[]` 及其 `types[]` 新增可选 `facts` 数组，已经按本地主洞见优先级排序。例如某范围内同一运动地点出现于 3 天：

```json
{
  "facts": [
    {
      "id": "<fact-sha256>",
      "kind": "recurring",
      "completedDurationSeconds": 10800,
      "recordCount": 3,
      "recordedDays": 3
    }
  ]
}
```

| 字段 | 含义 |
| --- | --- |
| `id` | 绑定范围、类型语义、汇总和来源记录的匿名指纹 |
| `kind` | `comparison`、`recurring` 或 `memorable` |
| `completedDurationSeconds` | 该事实证据中已完成记录的时长合计，整数秒 |
| `recordCount` / `recordedDays` | 该事实证据的记录数及不同自然日数 |
| `changeSeconds` | 仅 comparison 提供，有符号的本期减前期完成时长差 |

comparison 的证据同时包含两期，故其合计时长、记录数、天数是两期证据总和，不能解释成本期总量。本期总量读取所属 Period/TypeStats。recurring 的数值只代表被选中的同一个地点；memorable 只代表最长一条。所有整数秒向下取整，负变化按向零取整。不得把停留推断成有效工作、运动强度、睡眠或健康情况。

原始 UUID、地点名称、坐标、具体起止时刻和本地 `tipSources` 不编码到请求；指纹只用于匹配，不是可供模型解释的业务内容。

## 响应新增字段

仍需返回原有合法的 `day/week/periods` 和 `copy`。新增根字段：

```json
{
  "dailyTip": {
    "date": "2026-09-11",
    "sourceID": "<recent-source-sha256>",
    "text": "最近给运动留了时间，也记得按自己的步调来。"
  }
}
```

每个 Period 以及 `types[]` 元素可新增：

```json
{
  "insight": {
    "factID": "<fact-sha256>",
    "title": "日子的叠句",
    "body": "相似的段落再次落笔，回看时，日子便有了韵脚。"
  }
}
```

- tip `text` 为 1–40 个 Unicode scalar；`date` 必须等于请求日期，`sourceID` 必须等于请求 `recentSourceID`。
- 洞见 `title` 为 1–24 个 Unicode scalar，`body` 为 1–48 个；不可全为空白。`factID` 必须原样使用对应范围、对应类型的第一条候选事实 ID，不能混用总体与分类的 ID。
- 没有候选事实时省略 `insight`，不生成虚构事实。
- 新字段缺失、为空、结构错误、超长或指纹不匹配时，仅这项新能力回退本地。原有 V2 字段继续按原协议校验。
- 新文案不继承服务端配色，统计主卡保持本地暖色及可读性；原 `copy/style` 仅保留用于 V2 协议解码与校验，不再参与页面或海报渲染。每日 tip 不自动进入海报。

## 服务端表达规则

你在帮助用户温柔地回看时间。只使用客户端给出的摘要和所选事实；给一个真实发现配一句简短关照，不补写地点、人物、心理、原因或生活结论。

- 使用自然简体中文，短句、具体、不过度抒情。不要照搬“坚持就是胜利”之类口号，不轮询无关鸡汤。
- 不评价自律、懒惰、效率，不声称用户疲惫、焦虑或需要治疗；时长变少不等于偷懒，时长变多不等于更努力。
- tip 可以轻轻提出选择，用“也可以”“按自己的步调”等表达，不下命令，不制造落后或打卡压力。
- 零记录只温和邀请开始记录，不判断用户这几天没有活动。未记录的生活同样存在。
- 不把“3 个不同日子”改写为“连续 3 天”；不把某个地点的统计说成某类型的全部统计。
- 返回结构化 JSON；日期、类型、事实 ID、来源 ID 按协议原样对应。

## 缓存、更新与验收

客户端只持久化一份当前账户的每日关照和本地来源校验信息，键使用账户哈希；匹配日期与时区后读取。成功生成的 tip 当天不轮换，重启遇到网络失败仍复用。缺少新增字段或请求失败时，本地关照也可保存；后续成功重试可用有效智能文案替换本地兜底。周期文案保留在内存，重新启动继续沿用现有请求行为。

新增来源记录不会使已保存的当天关照换句。来源被修改、删除或类型改变后缓存失效；空数据关照在出现第一份近期记录后失效。账户切换取消请求、删除已验证当前账户缓存并清空内存，不能显示其他账户内容。网络中途更新记录时也重新校验来源。删除后若服务端返回当天旧幂等结果，来源指纹不匹配，显示本地关照。

客户端单元测试覆盖窗口、来源失效、重启与账户隔离、错误字段独立降级及事实匹配；渲染测试覆盖 320pt 小屏、浅色、深色和辅助功能大字体。服务端验收还需检查新增字段兼容、真实鉴权、幂等旧结果、生成约束及限流/超时；这些不由客户端模拟测试证明。

## 洞见文风与卡片主题

洞见正文从事实生发文学表达，统计数字与证据独立展示，不能把“工作反复出现”这样的事实概述当作洞见。无云端结果时，本地按比较、重复、较长停留提供简短手记；证据页仍保留原始事实标题。

统计卡片与分享海报跟随设置页选择的全局应用主题，选择通过 `appTheme` 保存到本机，切换不请求模型。`PeriodInsightCard`、`JournalPoster` 与 PNG 导出共用选择，默认隐藏地点名称保持不变。
